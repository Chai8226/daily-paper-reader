---
title: Optimal Solutions for the Moving Target Vehicle Routing Problem with Obstacles via Lazy Branch and Price
title_zh: 通过延迟分支定价算法求解带障碍物的动态目标车辆路径问题的最优解
authors: "Anoop Bhat, Geordan Gutow, Surya Singh, Zhongqiang Ren, Sivakumar Rathinam, Howie Choset"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21880v1"
tags: ["query:q2"]
score: 7.0
evidence: 多智能体避障轨迹规划
tldr: 本研究针对带障碍物的移动目标车辆路径问题（MT-VRP-O），提出了一种名为Lazy BPRC的优化算法。该问题要求多个智能体在避障及速度约束下拦截移动目标。由于在障碍物环境中计算移动目标间的运动规划成本极高，Lazy BPRC通过引入松弛连续性约束的下界来延迟成本计算，并利用凸集图（GCS）搜索最短路径。实验证明，该方法在保证最优性的同时，显著提升了求解效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.21880v1/fig-001.webp\", \"caption\": \"Fig. 1. Targets move through obstacle environment and must be intercepted within time windows, shown in bold-colored lines. Agents begin and end at depot, intercepting targets while avoiding obstacles.\", \"page\": 1, \"index\": 1, \"width\": 498, \"height\": 266}, {\"url\": \"assets/figures/arxiv/2603.21880v1/fig-002.webp\", \"caption\": \"Fig. 2. Computing bounds on the cost of an example tour Γ. To compute the lower bound c(Γ), we divide each target-window visited by Γ into segments. We then construct a segment-graph Gseg, where the nodes are the segments, and an edge connects every segment of Γ[n] to every segment of Γ[n+1]. The edge cost from segment ξ to ξ′ is the distance traveled along the shortest path in space from ξ to ξ′, if this path satisfies the relaxed timing constraints in Section V-D, and ∞ otherwise. c(Γ) is the cost of the shortest path in Gseg from ξ0 to ξ0 visiting all target-windows in Γ. To compute the upper bound c, we construct a segment-start-graph Gstart, where the nodes are the starting points of the segments, and an edge connects every segment-start of Γ[n] to every segment-start of Γ[n + 1]. The edge cost from s to s′ is distance traveled by a feasible minimum-distance agent trajectory from s to s′, if such a trajectory exists or if s′ = s0, and ∞ otherwise. Our upper bound is the cost of the shortest path in Gstart from s0 to s0 that visits all target-windows in Γ.\", \"page\": 4, \"index\": 2, \"width\": 498, \"height\": 660}, {\"url\": \"assets/figures/arxiv/2603.21880v1/fig-003.webp\", \"caption\": \"Fig. 3. (a) Varying the number of targets. Lazy BPRC shows smaller median runtime than the ablations, particularly for larger numbers of targets. (b) Varying the map resolution. Lazy BPRC’s advantage in median runtime grows as we increase the map resolution. (c) Varying the capacity. Lazy BPRC has smaller median runtime than the ablations for all tested capacities.\", \"page\": 8, \"index\": 3, \"width\": 498, \"height\": 467}]"
motivation: 在存在障碍物的情况下，计算移动目标之间满足避障要求的运动规划成本具有极高的计算复杂度。
method: 提出一种结合延迟评估的Branch-and-Price框架，利用松弛连续性约束计算成本下界，并基于凸集图搜索精确路径。
result: 实验表明，Lazy BPRC算法的运行速度比其他消融基准方法快了多达一个数量级。
conclusion: Lazy BPRC通过延迟计算策略有效解决了MT-VRP-O中的计算瓶颈，是求解复杂动态路径规划问题的有效工具。
---

## 摘要
带障碍物的动态目标车辆路径问题 (MT-VRP-O) 旨在为多个智能体寻找轨迹，以共同拦截一组移动目标。每个目标具有一个或多个必须访问的时间窗，且智能体必须避开静态障碍物并满足速度和容量限制。我们引入了具有松弛连续性的延迟分支定价算法 (Lazy BPRC)，用于求解 MT-VRP-O 的最优解。Lazy BPRC 采用了车辆路径问题 (VRP) 的分支定价框架，在受限主问题 (RMP) 和定价问题之间交替进行。RMP 旨在从有限的路径子集中为每个智能体选择一系列目标-时间窗配对（称为路径）。定价问题则向该有限子集中添加路径。通常情况下，求解 RMP 需要计算智能体遵循子集中每条路径的成本。在 MT-VRP-O 中，由于需要在移动目标之间进行无碰撞运动规划，计算这些成本的计算量非常巨大。Lazy BPRC 通过使用每条路径成本的下界来求解 RMP，从而延迟了成本计算，这些下界是通过松弛连续性约束的运动规划计算得到的。我们根据需要延迟评估路径的真实成本。我们通过在凸集图 (GCS) 上搜索最短路径来计算路径成本，并利用连续性松弛方法加速搜索。实验表明，Lazy BPRC 的运行速度比两种消融实验方法快一个数量级。

## Abstract
The Moving Target Vehicle Routing Problem with Obstacles (MT-VRP-O) seeks trajectories for several agents that collectively intercept a set of moving targets. Each target has one or more time windows where it must be visited, and the agents must avoid static obstacles and satisfy speed and capacity constraints. We introduce Lazy Branch-and-Price with Relaxed Continuity (Lazy BPRC), which finds optimal solutions for the MT-VRP-O. Lazy BPRC applies the branch-and-price framework for VRPs, which alternates between a restricted master problem (RMP) and a pricing problem. The RMP aims to select a sequence of target-time window pairings (called a tour) for each agent to follow, from a limited subset of tours. The pricing problem adds tours to the limited subset. Conventionally, solving the RMP requires computing the cost for an agent to follow each tour in the limited subset. Computing these costs in the MT-VRP-O is computationally intensive, since it requires collision-free motion planning between moving targets. Lazy BPRC defers cost computations by solving the RMP using lower bounds on the costs of each tour, computed via motion planning with relaxed continuity constraints. We lazily evaluate the true costs of tours as-needed. We compute a tour's cost by searching for a shortest path on a Graph of Convex Sets (GCS), and we accelerate this search using our continuity relaxation method. We demonstrate that Lazy BPRC runs up to an order of magnitude faster than two ablations.